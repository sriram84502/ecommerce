<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Users</title>
    <!-- Font Awesome -->
    <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    rel="stylesheet"
    />
    <!-- Google Fonts -->
    <link
    href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
    rel="stylesheet"
    />
    <!-- MDB -->
    <link
    href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.2.0/mdb.min.css"
    rel="stylesheet"
    />
    <link rel="shortcut icon" href="https://upload.wikimedia.org/wikipedia/en/a/a3/Logo_of_Andhra_Loyola_Institute_of_Engineering_and_Technology.png" type="image/x-icon">
</head>
<body style="background-color: #eeee;">
    <section style="background-color: #eee;">
        <div class="container py-5">
          <div class="row">
            <div class="col">
              <nav aria-label="breadcrumb" class="bg-light rounded-3 p-3 mb-3">
                <ol class="breadcrumb mb-0 d-flex" style="justify-content: space-between;">
                  <li class="breadcrumb-item"><a href="/admin" style="text-decoration: none;color: #000;">⬅️Back</a></li>
                  <div class="d-flex">
                    <li class="breadcrumb-item"><a href="/" style="text-decoration: none;color: black;">🌐Visit website</a></li>
                    <li class="breadcrumb-item"><a href="/logout" style="text-decoration: none;color: black;">↩Logout</a></li>
                  </div>
                </ol>
              </nav>
            </div>
          </div>
      
          <div class="row">
            <div class="col-lg-12">
              <div class="card mb-4">
                <div class="card-body text-center">
                  <h5 class="mb-3">Manage Users🙎‍♂️</h5>
                  <div class="row">
                    <div class="col-sm-1">
                      <h6 class="">SNo</h6>
                      <!-- <p class="mb-0">SNo</p> -->
                    </div>
                    <div class="col-sm-2">
                      <h6 class="">Name</h6>
                    </div>
                    <div class="col-sm-3">
                      <h6 class="">Email</h6>
                    </div>
                    <div class="col-sm-2">
                        <h6 class="">Last Login</h6>
                    </div>
                    <div class="col-sm-2">
                        <h6 class="">Is Active</h6>
                    </div>
                    <div class="col-sm-2">
                      <h6 class="">Manage</h6>
                    </div>
                  </div>
                  <hr>
                  <% if(sessions.length != 0) { %>
                  <% for (let i = 0; i < sessions.length; i++) { %>
                  <% if (sessions[i].userId) { %>
                  <div class="row">
                    <div class="col-sm-1">
                      <h6 class="text-muted"><%= i + 1 %></h6>
                    </div>
                    <div class="col-sm-2">
                      <h6 class="text-muted"><%= sessions[i].userId.name %></h6>
                    </div>
                    <div class="col-sm-3">
                      <h6 class="text-muted"><%= sessions[i].userId.email %></h6>
                    </div>
                    <% const dateToFormat = sessions[i].loginTime; %>
                    <div class="col-sm-2">
                        <h6 class="text-muted" id="formattedDate<%= i %>"></h6>
                        <script>
                            (function() {
                                const dateToFormat = "<%= dateToFormat %>";
                                function formatDate(inputDate) {
                                    const date = new Date(inputDate);
                                    const now = new Date();
                                    
                                    const isSameDay = date.getFullYear() === now.getFullYear() &&
                                                      date.getMonth() === now.getMonth() &&
                                                      date.getDate() === now.getDate();
                            
                                    if (isSameDay) {
                                        return date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }); // Format the time if it is the current date
                                    } else {
                                        return date.toLocaleDateString(); // Format the date if it is not the current date
                                    }
                                }
                                document.getElementById('formattedDate<%= i %>').textContent = formatDate(dateToFormat);
                            })();
                        </script>
                    </div>     
                    <div class="col-sm-2">
                        <% if (sessions[i].logoutTime == null) { %>
                            <h6 class="text-muted">🟢Online</h6>
                        <% } else { %>
                            <h6 class="text-muted">🔴Offline</h6>
                        <% } %>
                    </div>  
                    <div class="col-sm-2">
                      <a href="/delete-user/<%= sessions[i].userId._id %>"><button type="button" class="btn btn-info ms-1">🗑️Delete</button></a>
                    </div>
                  </div>
                  <hr>
                  <% }} %>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>
